	- The compiler to use is "clang++".
	
	- Your code has to be compiled with the following flags:
		"-Wall -Wextra -Werror"

	- Every output, goes to the "standard output".
  and will be ended by a "newline", unless specified otherwise.

	- Includes must contains every other includes they are depending on, obviously.

	- No coding style is enforced during in C++.

	- Files associated with a class will always be "ClassName.hpp" and "ClassName.cpp".

	- No "vectors", "lists", "maps", etc.

	- No "using namespace" and "friend".

	- No "*alloc", "free" and "*printf" fct.

